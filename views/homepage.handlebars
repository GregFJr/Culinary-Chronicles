{{> navbar }}
<h1>Culinary Chronicles</h1>
<div class="recipes-container">
    {{#each recipes}}
       <div class="recipe-card">
        <a href="/recipe/{{this.id}}" class="recipe-link">
            <img src="{{this.image_url}}" alt="{{this.title}}">
            <h3>{{this.title}}</h3>
            
            <!-- Star Rating System Here -->
            <!-- '+' Sign to Save Recipe Here -->
            </a>
        </div>
    {{/each}}
</div>

